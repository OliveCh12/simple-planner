# Simple Planner

Local-first planner made of calendars (stored as `plans`) holding objectives, tasks, subtasks and events. Persistence is IndexedDB in the browser. Agents should use the v3 JSON model.

- Agent API and MCP tools: docs/agent-api.md
- JSON Schema: schema/planner.schema.json
