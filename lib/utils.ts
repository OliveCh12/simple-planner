export { cn } from "cn";

/** Centered reading column — home, settings, item page. */
export function containerClasses() {
  return "mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8";
}

/** Full-width chrome aligned with the calendar surface. */
export function shellClasses() {
  return "w-full px-4 sm:px-6 lg:px-8";
}
